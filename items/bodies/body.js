class Body {
  constructor() {
    this.name = "Whatever";
    this.slot = "body";
    //this.color = color(0); // doesn't matter man
    this.properties = {
      damage: 10,
    }
  }

  /* show() { // no need to draw something
     stroke(this.color);
     ellipse(width / 2, height / 2, 150, 150);
   }*/
}